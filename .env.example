# ============================================================================
# Code2Video Environment Variable Configuration
#
# Usage: Copy this file to .env, then modify the corresponding values
#   cp .env.example .env
# ============================================================================

# --------------------------------------------------------------------------
# API Authentication Keys (must be included in request headers when calling from frontend)
# Multiple keys separated by commas
# --------------------------------------------------------------------------
API_KEYS=dev-api-key-12345

# --------------------------------------------------------------------------
# Default LLM API (options: claude, gpt4o, gpt-41, gpt5, gemini)
# --------------------------------------------------------------------------
DEFAULT_API=claude

# --------------------------------------------------------------------------
# Port Configuration
# --------------------------------------------------------------------------
API_PORT=8080
REDIS_PORT=6379

# --------------------------------------------------------------------------
# Concurrency Configuration (leave empty for auto-detection)
# --------------------------------------------------------------------------
MAX_WORKERS=

# --------------------------------------------------------------------------
# Debug Mode (set to false in production)
# --------------------------------------------------------------------------
DEBUG=false
